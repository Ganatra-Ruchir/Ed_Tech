/** Shared hex values for the maroon "Silver Oak University" student theme
 * — used both in Tailwind arbitrary-value classes and directly in chart
 * (recharts) styling, which needs real hex rather than CSS classes. */
export const MAROON = {
  950: "#3e0917",
  900: "#4a0b1d",
  800: "#5c0f24",
  700: "#6b1029",
  600: "#7c1638",
  500: "#93213f",
  400: "#b83a58",
} as const;
